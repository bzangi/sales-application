package org.bzangi.repository;

import org.bzangi.model.Client;
import org.springframework.stereotype.Repository;

@Repository
public class ClientRepository {
    public void save(Client client) {
        // access DB and saves the client
    }
}
